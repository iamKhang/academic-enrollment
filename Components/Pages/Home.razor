@page "/"

<PageTitle>Home</PageTitle>

<section class="mx-auto flex max-w-3xl flex-col gap-6 py-12">
    <header class="space-y-2">
        <h1 class="text-3xl font-semibold text-slate-900">AcademicEnrollment</h1>
        <p class="text-slate-600">The app is ready for your components.</p>
    </header>

    <MudPaper Class="flex flex-col gap-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm" Elevation="0">
        <MudText Typo="Typo.h6" Class="text-slate-900">MudBlazor is ready</MudText>
        <MudText Class="text-slate-600">Tailwind utility classes are available too.</MudText>
        <MudButton Variant="Variant.Filled"
                   Color="Color.Primary"
                   Class="inline-flex w-fit items-center focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:!outline-blue-500">
            Get started
        </MudButton>
    </MudPaper>
</section>
